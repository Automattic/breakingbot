{quote}{{data.description}}{quote}

_Action item created{{#if data.accountId}} by {{data.accountId}}{{/if}} as part of breaking incident {{data.issueKey}}._

{{#if data.contextUrl}}_See [Slack context|{{data.contextUrl}}] for more details._{{/if}}